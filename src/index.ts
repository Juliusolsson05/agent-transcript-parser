// The root API is the evidence-driven engine. `./v2` remains as an explicit
// migration alias, while the frozen provisional-render ledger stays isolated
// behind `agent-transcript-parser/ghost`.
export * from './v2/index.js'
